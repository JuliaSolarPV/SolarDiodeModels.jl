"""Single diode equations and models."""

"""    SingleDiode
@component function SingleDiode(; name)
    # @variables in(t) out(t)
    # eqs = [out ~ in + 1]
    # return System(eqs, t, [in, out], []; name)
end